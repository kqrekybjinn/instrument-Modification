# M0603A 串口指令结构（本工程使用）

本工程发送的电机控制帧固定为 **10 字节**：

| Byte | 名称 | 说明 |
|---:|---|---|
| 0 | ID | 设备地址，本工程默认 `0x01` |
| 1 | CMD | 指令码 |
| 2..8 | D0..D6 | 7 字节数据区（不同 CMD 含义不同） |
| 9 | CRC | CRC-8/MAXIM 校验（对前 9 字节计算） |

## CRC

CRC 算法：**CRC-8/MAXIM**

- Poly: `0x31`（反射实现中等价使用 `0x8C`）
- Init: `0x00`
- RefIn/RefOut: true
- XorOut: `0x00`
- 计算范围：`Byte0..Byte8`（共 9 字节）

工程实现函数：`crc8_maxim()`（见 main/motor_driver.c）。

## 指令示例

以下均以 ID=`0x01` 为例。

### 1) 使能

```
01 A0 08 00 00 00 00 00 00 6F
```

- CMD=`0xA0`
- D0=`0x08`
- D1..D6=`0x00`
- CRC=`0x6F`

### 2) 速度环模式

```
01 A0 02 00 00 00 00 00 00 E4
```

- CMD=`0xA0`
- D0=`0x02`
- D1..D6=`0x00`
- CRC=`0xE4`

### 3) 转速 30rpm

```
01 64 01 2C 00 00 00 00 00 A6
```

- CMD=`0x64`
- D0..D1：速度值（**int16，大端**）
  - `0x012C` = 300
  - 本工程约定 `rpm10` 为 **0.1rpm** 单位，因此 30.0rpm → `rpm10=300`
- D2..D6：保留，置 0
- CRC=`0xA6`

### 4) 停止

```
01 64 00 00 00 00 00 00 00 50
```

- CMD=`0x64`
- D0..D6 全 0
- CRC=`0x50`

## 本工程初始化时序

初始化时序在 main/app_main.c：

- 上电后延时 1s：发送“使能”
- 再延时 100ms：发送“速度环模式”
