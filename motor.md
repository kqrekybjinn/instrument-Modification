# M0603A 电机串口通讯协议

## 基本参数

| 参数 | 值 |
|------|-----|
| 波特率 | 38400 bps，8N1 |
| 帧长度 | 10 字节（固定） |
| 接口 | 单线半双工（LIN 总线） |
| 通讯模式 | 一问一答（每条发送指令均有对应应答） |
| 最高指令频率 | 250 Hz |
| CRC 算法 | CRC-8/MAXIM（Poly=0x31，反射，Init=0x00，XorOut=0x00） |

---

## 帧结构

```
┌────────┬────────┬──────────────────────┬────────┐
│ Byte 0 │ Byte 1 │    Byte 2 ~ Byte 8   │ Byte 9 │
│  ID    │  CMD   │       数据区          │  CRC8  │
└────────┴────────┴──────────────────────┴────────┘
```

- **ID**：电机设备地址，本工程默认 `0x01`
- **CMD**：指令码
- **数据区**：7 字节，含义依 CMD 而定
- **CRC8**：对 Byte0～Byte8 共 9 字节计算

### CRC-8/MAXIM 计算方式

```c
uint8_t crc8_maxim(const uint8_t *data, size_t len) {
    uint8_t crc = 0x00;
    for (size_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (uint8_t bit = 0; bit < 8; bit++)
            crc = (crc & 0x01) ? (crc >> 1) ^ 0x8C : crc >> 1;
    }
    return crc;
}
```

---

## 指令总表

### 主机 → 电机（发送）

| CMD  | 功能 | 对应应答 |
|------|------|----------|
| 0x64 | 驱动指令（速度/电流/位置给定） | 0x65 |
| 0x74 | 查询里程圈数与当前角度 | 0x75 |
| 0xA0 | 模式切换 | 0xA1 |

### 电机 → 主机（应答）

| CMD  | 对应请求 | 功能 |
|------|----------|------|
| 0x65 | 0x64 | 返回实际速度、电流、温度、故障码 |
| 0x75 | 0x74 | 返回里程圈数（int32）、当前角度（uint16）、故障码 |
| 0xA1 | 0xA0 | 返回模式切换确认（回显模式值） |

---

## 1. 驱动指令 0x64 → 应答 0x65

### 发送帧

| 字节 | 字段 | 说明 |
|------|------|------|
| [0] | ID | 设备地址 |
| [1] | 0x64 | 驱动指令码 |
| [2] | 给定值高字节 | **有符号 int16，大端序** |
| [3] | 给定值低字节 | 不同模式下含义不同，见下表 |
| [4] | 0x00 | 保留 |
| [5] | 0x00 | 保留 |
| [6] | 加速时间 | 速度环有效；每升 1 rpm 所需 ms 数，0 = 默认 1ms |
| [7] | 刹车标志 | 速度环有效；0xFF = 刹车停止，其他 = 正常驱动 |
| [8] | 0x00 | 保留 |
| [9] | CRC8 | |

**给定值范围（Byte2~3，int16）：**

| 控制模式 | 数值含义 | 范围 |
|----------|----------|------|
| 速度环 | 0.1 rpm/unit，正转为正 | −3800 ~ +3800（对应 −380 ~ +380 rpm） |
| 电流环 | raw，正转为正 | −32767 ~ +32767（对应 −4A ~ +4A） |
| 位置环 | 0.01095°/unit（0~32767 = 0°~360°），无符号含义 | 0 ~ 32767 |

**典型帧示例（速度环）：**

```
正转 30 rpm：  01 64 01 2C 00 00 00 00 00 A6
              rpm10 = 300 = 0x012C
正转 60 rpm：  01 64 02 58 00 00 00 00 00 50
正转 90 rpm：  01 64 03 84 00 00 00 00 00 26
反转 30 rpm：  01 64 FE D4 00 00 00 00 00 63
停止（速度=0）：01 64 00 00 00 00 00 00 00 50
刹车停止：     01 64 00 00 00 00 00 FF 00 D1
```

### 应答帧 0x65

| 字节 | 字段 | 说明 |
|------|------|------|
| [0] | ID | |
| [1] | 0x65 | 应答标识 |
| [2] | 实际速度高字节 | 有符号 int16，0.1 rpm/unit |
| [3] | 实际速度低字节 | |
| [4] | 实际电流高字节 | 有符号 int16 |
| [5] | 实际电流低字节 | |
| [6] | 加速时间 | |
| [7] | 绕组温度 | 单位 °C |
| [8] | 故障码 | 见故障码表 |
| [9] | CRC8 | |

---

## 2. 里程查询 0x74 → 应答 0x75

用于获取电机累计里程圈数（上电清零）和当前角度位置。

### 发送帧

```
01 74 00 00 00 00 00 00 00 XX   （XX = CRC8）
```

| 字节 | 字段 | 说明 |
|------|------|------|
| [0] | ID | |
| [1] | 0x74 | 查询指令 |
| [2]~[8] | 0x00 | 数据区全0 |
| [9] | CRC8 | |

### 应答帧 0x75

| 字节 | 字段 | 说明 |
|------|------|------|
| [0] | ID | |
| [1] | 0x75 | 应答标识 |
| [2] | 里程圈数 Byte3（最高位） | **有符号 int32，大端序** |
| [3] | 里程圈数 Byte2 | 正转为正，上电清零 |
| [4] | 里程圈数 Byte1 | 范围：−2,147,483,647 ~ +2,147,483,647 |
| [5] | 里程圈数 Byte0（最低位） | |
| [6] | 当前角度高字节 | **无符号 uint16，大端序** |
| [7] | 当前角度低字节 | 0 ~ 32767 对应 0° ~ 360° |
| [8] | 故障码 | 见故障码表 |
| [9] | CRC8 | |

**里程圈数解析（C 代码）：**
```c
int32_t mileage = (int32_t)(
    ((uint32_t)buf[2] << 24) |
    ((uint32_t)buf[3] << 16) |
    ((uint32_t)buf[4] <<  8) |
     (uint32_t)buf[5]);
```

**当前角度解析（C 代码）：**
```c
uint16_t angle_raw = (uint16_t)(((uint16_t)buf[6] << 8) | buf[7]);
float angle_deg = angle_raw * 360.0f / 32767.0f;
```

---

## 3. 模式切换 0xA0 → 应答 0xA1

### 发送帧

```
01 A0 <MODE> 00 00 00 00 00 00 XX
```

| Byte[2] 模式值 | 功能 |
|----------------|------|
| 0x00 | 开环驱动 |
| 0x01 | 电流环 |
| 0x02 | 速度环 |
| 0x03 | 位置环（0~32767 = 0°~360°，掉电后回零） |
| 0x08 | 电机使能（默认进入电流环） |
| 0x09 | 电机失能 |
| 0x0A | 电机后转 150±10°（校准用） |
| 0x10 | 开启通讯断联保护（超过 3s 无指令自动停止） |
| 0x11 | 关闭通讯断联保护 |

**典型帧示例：**

```
使能：           01 A0 08 00 00 00 00 00 00 6F
速度环：         01 A0 02 00 00 00 00 00 00 E4
位置环：         01 A0 03 00 00 00 00 00 00 D9
失能：           01 A0 09 00 00 00 00 00 00 6A
后转校准：       01 A0 0A 00 00 00 00 00 00 81
```

### 应答帧 0xA1

```
01 A1 <MODE> 00 00 00 00 00 00 XX   （回显模式值确认）
```

---

## 故障码（Byte8）

| 位 | 含义 |
|----|------|
| BIT0 | 霍尔故障 |
| BIT1 | 过流故障 |
| BIT2 | 保留 |
| BIT3 | 堵转故障 |
| BIT4 | 过温故障 |
| BIT5 | 通讯断联故障 |
| BIT6 | 过/欠压故障 |
| BIT7 | 保留 |

---

## 本工程使用序列

### 上电初始化
```
延时 5s（界面初始化）
→ 01 A0 08 ...  使能
→ 延时 100ms
→ 01 A0 02 ...  切速度环
```

### 计时模式运行
```
→ 01 64 <RPM_H> <RPM_L> 00 00 00 00 00 <CRC>   正转
→ 01 64 00 00 00 00 00 00 00 50                 停止
→ 01 64 <-RPM_H> <-RPM_L> 00 00 00 00 00 <CRC> 反转（时长根据正转时长反比例估算）
→ 01 64 00 00 00 00 00 00 00 50                 停止回零
```

### 圈数模式运行（里程反馈）
```
→ 01 74 00 00 00 00 00 00 00 <CRC>             查询初始里程（记为 mileage_start）
→ 01 64 <RPM_H> <RPM_L> 00 00 00 00 00 <CRC>  正转
  → 每 50ms 查询 0x74，读取 mileage_now
  → 当 mileage_now - mileage_start >= target_count 时刹车
→ 01 64 00 00 00 00 00 00 00 50                刹车
→ 01 64 <-RPM_H> <-RPM_L> 00 00 00 00 00 <CRC> 反转
  → 每 50ms 查询 0x74
  → 当 mileage_now <= mileage_start 时刹车回零
→ 01 64 00 00 00 00 00 00 00 50                刹车
```

### UART 时序要求
- 发送帧后约 **2.6ms**（38400 baud × 10 bytes）电机收到完整帧
- 电机处理 + 应答耗时约 **3ms**
- 建议主机等待 **15ms** 后读取应答，超时视为通讯失败
- 单次查询总耗时（TX + 等待 + RX）< 20ms，50ms 轮询周期完全满足
